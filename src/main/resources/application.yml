micronaut:
  application:
    name: crudApi
    datasources:
  default:
    url: jdbc:mysql://localhost:3306/bottal
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    schema-generate: CREATE_DROP
    dialect: MYSQL
jpa.default.properties.hibernate.hbm2ddl.auto: update

